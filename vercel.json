{
  "version": 2,
  "buildCommand": "npm run build",
  "devCommand": "npm run app",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.ts" }
  ]
}
